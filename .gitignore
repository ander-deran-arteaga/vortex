# deps
node_modules/

# builds
dist/
.next/
out/
coverage/
*.tsbuildinfo

# foundry
cache/
broadcast/
deployments/*fork*.json

# env
.env
.env.local
.env.*.local

# runtime data (append-only JSON store, logs)
apps/api/data/
*.log

# agent coordination (supervised by master-agent, never committed)
coordination/
.agents/

# throwaway investigation tests — probes belong in the scratchpad, never in the
# committed suite. Name them *.probe.test.ts / scratch.* and they cannot leak.
*.probe.test.ts
**/scratch.*.test.ts

# misc
.DS_Store

# detached anvil logs from scripts/ensure-demo.sh
.anvil-*.log
.verify-*.pid
